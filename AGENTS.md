# Agent entrypoint

如果用户把这个仓库交给你，但没有明确要求开发或安装 App，不要默认他的目标是构建软件。

先阅读 [MEMORY_AGENT.md](MEMORY_AGENT.md)。App 只是这套记忆系统的一个入口；你也可以直接按照那份说明，在用户选择的本地文件夹里成为他的记忆维护助手。

只有当用户明确提出修改代码、构建客户端或安装 App 时，才把它当作软件工程任务。
